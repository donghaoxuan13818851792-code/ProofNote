{
  "name": "proofnote",
  "version": "3.1.0-rc.1",
  "description": "Proofnote — structured math, without the JSON pain. An editor, validator and renderer for the Solution Note format.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --check src/doc-page.js && node tests/regression.js && node tests/i18n.js && node tests/document-model.js && node tests/document-store.js && node tests/document-editor.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^29.1.1"
  }
}
